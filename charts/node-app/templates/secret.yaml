{{- if .Values.secrets.data }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.deployment.name }}
data:
  {{- range $key, $val := .Values.secrets.data }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
{{- end }}